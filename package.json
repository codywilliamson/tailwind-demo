{
  "name": "tailwind-demo",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && echo 'Build complete! Upload the dist folder to Netlify or run: netlify deploy --prod --dir=dist'"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.3"
  }
}
